{
    "name": "spatie/mailcoach-sdk-php",
    "description": "An SDK for using the Mailcoach API in PHP",
    "keywords": [
        "spatie",
        "mailcoach-sdk-php"
    ],
    "homepage": "https://github.com/spatie/mailcoach-sdk-php",
    "license": "MIT",
    "authors": [
        {
            "name": "Freek Van der Herten",
            "email": "freek@spatie.be",
            "role": "Developer"
        }
    ],
    "require": {
        "php": "^8.1"

    },
    "require-dev": {
        "guzzlehttp/guzzle": "^7.0",
        "laravel/pint": "^1.2",
        "pestphp/pest": "^1.20",
        "pestphp/pest-plugin-mock": "^1.0",
        "phpstan/phpstan": "^1.9",
        "spatie/ray": "^1.28",
        "vlucas/phpdotenv": "^5.5"
    },
    "suggest": {
        "guzzlehttp/guzzle": "Make HTTP requests"
    },
    "autoload": {
        "psr-4": {
            "Spatie\\MailcoachSdk\\": "src"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "Spatie\\MailcoachSdk\\Tests\\": "tests"
        }
    },
    "scripts": {
        "test": "vendor/bin/pest",
        "analyse": "vendor/bin/phpstan",
        "test-coverage": "vendor/bin/pest --coverage",
        "format": "vendor/bin/pint"
    },
    "config": {
        "sort-packages": true,
        "allow-plugins": {
            "phpstan/extension-installer": true,
            "pestphp/pest-plugin": true
        }
    },
    "minimum-stability": "dev",
    "prefer-stable": true
}
